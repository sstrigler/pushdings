%%-*- mode: erlang -*-
{application, pushdings,
 [{description, "Pusheldy Dingseldey"},
  {vsn, "0.0.1"},
  {modules, []},
  {registered, [pushdings_sup]},
  {mod, {pushdings, []}},
  {applications, [kernel,
                  stdlib,
                  lager,
                  mnesia,
                  cowboy,
                  gproc,
                  hackney,
                  jsx]},
  {env, [{ssl, false},
         {tcp_opts, [{port, 8080}]}]}
 ]}.
